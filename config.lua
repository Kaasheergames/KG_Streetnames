Config = {}

Config.Locale = 'nl' --Language of choice, must be inside locales folder

Config.BlacklistedCars = { --Blacklisted vehicles. UI wont show up.
    --Template: [(number)] = "(vehicle name)",
    [1] = "BMX",
}

Config.StatusOnStartup = false --Says that the UI is automaticly on when starting the script.